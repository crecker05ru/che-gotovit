import React from 'react'

export default function EditRecipe() {
  return (
    <div>
       {/* <div className="recipe-card__body">
        <h3 className="recipe-card__title">Title: {title}</h3>
        <div className="recipe-card__icon edit-icon" onClick={editHandler}></div>
        <div className="recipe-card__image"><img src={image} alt={title}></img></div>
        <div className="recipe-card__weight">Weight: {weight}</div>
        <div className="recipe-card__totalTime">Total time: {totalTime}</div>
        <div className="recipe-card__quantity">Quantity: {quantity}</div>
        <div className="recipe-card__measure">Measure: {measure}</div>
        <ul className="recipe-card__ingredients">
        {ingredients.length > 0  && ingredients.map((ingredient )=> <li className="recipe-card__ingredient" key={ingredient.title}>
          <Ingredient {...ingredient} />
        </li>)}
        </ul>
        <ul className="recipe-card__steps">
        {steps.length > 0 && steps.map((step) => <li className="recipe-card__step" key={step}>{step}</li>)}
        </ul>
      </div> */}
    </div>
  )
}
